#!/bin/bash

# 定义账号别名
profiles=("dev" "staging" "prod" "dx-stg" "dx-prod")

# 常用 AWS 区域（可自定义）
regions=("ap-northeast-1" "us-east-1" "us-west-2")

# 输出文件夹
mkdir -p lambda_export

for profile in "${profiles[@]}"; do
  for region in "${regions[@]}"; do
    echo "📍 Scanning [$profile] in region [$region]..."
    aws lambda list-functions \
      --profile $profile \
      --region $region \
      --query "Functions[*].[FunctionName,Runtime,LastModified]" \
      --output text >> lambda_export/lambda_${profile}_${region}.txt
  done
done

echo "✅ 全部导出完成，文件位于 ./lambda_export/"
