# 需要安装Microsoft.Graph.Intune模块
Get-MgDeviceManagementManagedDevice -Filter "operatingSystem eq 'Windows'" | 
Select-Object deviceName, processorModel, totalStorageSpace, physicalMemoryInBytes

  tags = {
    Name        = "eol-dev-terraform-state"
    Project     = "eol"
    Environment = "dev"
    Purpose     = "terraform-state"
    ManagedBy   = "Terraform"
  }

お疲れ様です。

AWS環境構築について、AWSアカウント作成、Terraformの基本構成作成、およびTerraform State保存用のS3バケット作成まで完了しています。

Budget、ユーザー／グループおよび権限設定については、本日中に完了予定です。

一方、以下の構築については、必要な情報がまだ未確定のため、現時点では作業を完了できない状況です。

Amplify：GitHubまたはGitLabのRepository情報が必要
Route 53：DEV環境で使用するドメイン情報が必要
CloudFront：AmplifyをOriginとして設定する必要があるため、Amplifyの構築完了が前提となります。また、HTTPS設定に使用するACM証明書もドメイン情報に依存するため、上記情報の確定後に構築可能となります。

上記については鈴木さんへ確認のメッセージを送付済みです。
必要な情報をいただき次第、Amplify、Route 53、ACM、CloudFrontの構築を順次進める予定です。

よろしくお願いいたします。
